import { PosterImageValidationDirective } from './poster-image-validation.directive';

describe('PosterImageValidationDirective', () => {
  it('should create an instance', () => {
    const directive = new PosterImageValidationDirective();
    expect(directive).toBeTruthy();
  });
});
